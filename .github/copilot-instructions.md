# Copilot Instructions

## Pokyny k projektu
- Uživatel preferuje, aby pomocné typy nebyly vnořené ve `MainWindow`, ale jako samostatné entity v `Models` nebo se použily existující modely; a aby filtry (např. kategorie) byly načítané z databáze, ne hardcoded.